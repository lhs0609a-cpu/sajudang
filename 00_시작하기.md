# 시작하기 — 터미널에서 바로

```bash
mkdir sajudang && cd sajudang && git init
mkdir -p docs seed reference tools

# 이 폴더의 파일을 배치
cp .../성신당_개발문서/*.md              docs/
mv docs/CLAUDE.md docs/TASKS.md          ./          # 루트로
cp .../성신당_개발문서/seed/*.json       seed/
cp .../성신당_개발문서/sajudang_참조구현체.html  reference/sajudang.html
cp -r .../성신당_개발문서/scaffold/*     ./

claude
```

첫 프롬프트

```
CLAUDE.md 와 TASKS.md 를 읽고, T1-1 부터 시작해줘.
```

---

## 폴더 구성

```
sajudang/
├─ CLAUDE.md          ★ Claude Code 가 매 세션 읽는 컨텍스트
├─ TASKS.md           ★ 태스크 지시서 — 그대로 붙여넣기
├─ Makefile
├─ docs/              00~13 설계 문서 14종
├─ seed/              bank / lenses / relay_rules / guard / ilgan / meta
├─ reference/
│   └─ sajudang.html  동작하는 참조 구현체 (문서와 어긋나면 이게 정답)
├─ services/api/engine/
│   ├─ calendar.py    골격 (sxtwl 연결 필요)
│   └─ guard.py       완성
├─ tools/
│   ├─ distribution.py  분포 검증
│   └─ dup_rate.py      중복률 측정
└─ tests/fixtures/     회귀 케이스 (기대값 직접 채울 것)
```

---

## 순서

```
1주   T1-1 스캐폴드 · T1-2 만세력
2주   T1-3 검증 · T1-4 Feature Store    ← 여기 통과 전엔 UI 금지
3주   T2-1~3 문장엔진 + 가드
4주   T3-1~3 DB + API
5~7주 T4-1~4 프론트
8주   T5-1~2 결제 + 리텐션
```

---

## 2주차 관문

```bash
make engine-check
```

셋 다 통과해야 다음으로 갑니다.

```
pytest tests/test_calendar.py    고정 50건 전량 일치
python tools/distribution.py     0% 인 값 없음
python tools/dup_rate.py         중복률 15% 이하
```

**사주 8글자가 한 글자라도 기존 만세력과 다르면 그 위에 쌓는 게 전부 무의미합니다.**

---

## 지금 비어 있는 것

| | 상태 |
|---|---|
| `calendar.py` 절기 함수 | `NotImplementedError` — sxtwl 연결 필요 |
| `features.py` | 미작성 (T1-4) |
| `bank.py` `lens.py` `relay.py` | 미작성 (T2, T3) |
| `tests/fixtures/charts.json` | 기대값 직접 입력 필요 |
| 에셋 | 0/24 장면, 0/20 캐릭터 |

로직은 전부 `reference/sajudang.html` 안에 동작하는 형태로 있습니다.
포팅하면서 **절기·대운수 근사식만 정식 계산으로 교체**하면 됩니다.
