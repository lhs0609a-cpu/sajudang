"""
근거의 **이치** — 무엇을 보고 어떤 규칙으로 그렇게 읽었는가.

★ 왜 이 파일이 생겼나

  근거 줄을 세어 보니 이랬습니다 (tools/evidence_audit.py) —

      관측  87.6%      여덟 글자에서 무엇을 읽었는지
      이치   0.0%      그것을 **어떤 규칙**으로 읽었는지
      결론  17.6%
      출처   9.2%

  열에 여덟이 **읽은 것을 나열했을 뿐**이었습니다.

      돈 → 재성이 하나 · 가장 센 자리 식상
      통근 없음 · 정관

  손님은 이걸 보고 「그래서 뭐」 라고 묻습니다. 관측만 대면 그건
  근거가 아니라 **자료**입니다.

★ 「과학적으로 입증」 은 못 씁니다

  사주는 과학적으로 검증된 적이 없습니다. 그렇게 쓰면 거짓말이고
  이 집이 금지한 것입니다 (docs/11). 적중률·통계도 마찬가지입니다.

  회의적인 독자를 설득하는 것은 그 말이 아니라 **따라갈 수 있는
  논증**입니다. 무엇을 보고(관측), 어떤 이치로(규칙), 그래서 이렇게
  (결론), 그 이치는 어디서 왔는가(출처).

  이 넷이 다 있으면 독자는 **어디가 틀렸는지 짚을 수 있습니다.**
  짚을 수 있는 말이라야 믿을 수 있는 말입니다.

★ 출처를 지어내지 않습니다

  「자평진전 42쪽」 같은 것은 안 씁니다. 확인할 수 없는 인용은
  거짓과 같습니다. 갈래 이름까지만 댑니다 — 여기 적힌 규칙은 전부
  자평(子平) 명리에서 쓰는 것이고, 그건 사실입니다.
"""
from __future__ import annotations

# ── 십신 — 그대를 가운데 두고 여덟 글자의 관계에 붙인 이름 ─────
#
#   규칙을 **한 줄로** 적습니다. 길면 아무도 안 읽고, 짧으면 이치가
#   안 보입니다.
# ★ 쉬운 말로 (2026-09-11 · docs/21). 「같은 자리」 「결이 다른 자리」 는
#   근거인데 손님이 못 읽었습니다. **무엇인지**를 대고 무엇을 보는지 적습니다.
TEN_GOD = {
    "비견": "비견은 그대와 같은 편에 선 친구·동료라, 겨루는 일과 나눠 갖는 몫을 보오",
    "겁재": "겁재는 그대 몫을 나눠 가져가는 사람이라, 뺏기고 나누는 일을 보오",
    "식신": "식신은 그대가 만들어 남에게 내놓는 재주라, 만드는 일과 먹고사는 복을 보오",
    "상관": "상관은 그대가 참지 않고 내놓는 말과 재주라, 재주와 윗사람과의 부딪힘을 보오",
    "편재": "편재는 크게 들어오고 크게 나가는 돈이라, 크게 오가는 돈을 보오",
    "정재": "정재는 월급처럼 꼬박꼬박 모이는 돈이라, 모으고 지키는 돈을 보오",
    "편관": "편관은 그대를 몰아붙이는 압박이라, 급한 일과 마감을 보오",
    "정관": "정관은 그대가 지켜야 할 규칙과 직책이라, 직책과 규칙을 보오",
    "편인": "편인은 남다른 것을 혼자 파고드는 공부라, 남다른 배움과 외로움을 보오",
    "정인": "정인은 그대를 챙겨 주는 어른과 배움이라, 배우고 기대는 힘을 보오",
}

# 십신을 다섯으로 묶은 이름
GROUP = {
    "비겁": "비겁은 친구·동료처럼 그대 편에 선 것이라, 겨루는 일과 나눠 갖는 몫을 보오",
    "식상": "식상은 말·글·솜씨처럼 그대가 내놓는 것이라, 표현하는 일과 돈 버는 길을 보오",
    "재성": "재성은 돈·살림처럼 그대가 쥐는 것이라, 돈과 쥐는 힘을 보오",
    "관성": "관성은 회사·규칙처럼 그대를 누르는 것이라, 직책과 규칙과 압박을 보오",
    "인성": "인성은 공부·어른처럼 그대를 채워 주는 것이라, 배움과 기대는 힘을 보오",
}

# ── 그 밖의 이치 ─────────────────────────────────────────
RULE = {
    "통근있음": "위 글자가 아래 글자에 뿌리를 두면 잘 버틴다고 읽소",
    "통근없음": "위 글자가 아래 글자에 뿌리가 없으면 잘 흔들린다고 읽소",
    "신강": "그대를 돕는 힘이 누르는 힘보다 세면 신강이라 하오",
    "신약": "그대를 누르는 힘이 돕는 힘보다 세면 신약이라 하오",
    "중화": "돕는 힘과 누르는 힘이 비슷하면 중화라 하오",
    "용신": "그대에게 모자란 것을 채워 주는 기운을 용신이라 하고, 그걸 "
            "어디서 얻는지를 보오",
    # ★ 이치 자리에서 **뜻풀이를 하지 않습니다** (2026-09-25).
    #
    #   네 줄이 풀이표(`terms.MEANING`)와 같은 말로 시작해, 화면에
    #   「월지(태어난 달의 아래 글자, 곧 태어난 계절)는 태어난 달의 아래
    #   글자, 곧 태어난 계절이라…」 가 나갔습니다. 한 줄이 두 배로 길어진
    #   채 같은 말을 두 번 하니 손님은 둘 다 흘립니다.
    #
    #   풀이는 괄호가 맡고, 이 자리는 **왜 그렇게 읽는지**만 댑니다.
    "월지": "태어난 계절이 여덟 글자 가운데 힘이 가장 세다고 보오",
    "일지": "일지는 그대 발밑 글자라, 배우자와 가장 가까운 사람과의 일을 보오",
    "지장간": "눈에 안 보이는 기운도 셈에 넣소",
    "대운": "여덟 글자는 안 바뀌고 십 년마다 주인공만 바뀌오",
    "절기": "계절이 바뀌는 날의 그 시각으로 달을 가르오. 날짜가 아니라 "
            "시각이오",
    "궁위": "년·월·일·시 네 기둥을 각각 윗대·부모·그대·자식으로 보오",
    "대조": "성향 네 글자를 여덟 글자에서도 뽑아, 그대가 적은 것과 견줘 "
            "어긋나는 곳을 보오",
    # 마무리 컷에 씁니다 — **무엇이 안 바뀌는지**를 말해야 값이 섭니다.
    "명식": "여덟 글자는 태어난 때에 정해져 평생 안 바뀌오. 바뀌는 "
            "것은 십 년마다 도는 대운이오",
    "신살": "좋다 나쁘다를 정하는 이름이 아니라 어디를 볼지 가리키는 "
            "이름으로 읽소",
    # 척추 컷에 씁니다 (engine/spine) — 힘이 생의 고리를 따라 어디까지 닿는가.
    "흐름": "힘이 가장 많이 가는 곳을 흐름이라 하오. 그 다음 차례 글자가 "
            "여덟 글자에 있으면 힘이 거기까지 이어진다고 읽소",
}

# ── 관점 컷의 축 — 축 하나하나가 명리의 읽는 틀입니다 ─────
#
#   리포트 컷은 축 셋을 곱해 만듭니다. 그런데 근거 줄에는 **무엇을
#   봤는지**만 있고 그 축이 무엇을 재는 자리인지가 없었습니다.
#   축의 뜻을 한 줄로 답니다.
AXIS = {
    # ★ 손님이 고른 자리. 여덟 글자에서 뽑은 축이 아니라 **밖에서
    #   들어온** 축이라, 이치도 그렇게 적습니다 — 무엇을 보고 읽는지.
    "concern": ("물으신 일을 여덟 글자의 어느 글자로 읽을지 먼저 정하오. "
                "같은 사주도 무엇을 물었는지에 따라 보는 곳이 다르오", "십신"),
    "deuk": ("태어난 계절과 그대 발밑 글자가 그대를 돕는지를 보오. 둘 다 "
             "그대 편이면 잘 버티오", "강약"),
    "johu": ("춥고 더움을 보오. 겨울에 난 쇠는 불을 만나야 쓸모가 나오", "용신"),
    "seupjo": ("젖고 마름을 보오. 너무 마르거나 너무 젖으면 쓰기 어렵소",
               "용신"),
    "gap_band": ("가장 많은 기운과 가장 적은 기운의 차이를 보오. 차이가 "
                 "클수록 한쪽으로 쏠린 사주요", "강약"),
    "score_band": ("돕는 힘과 누르는 힘을 재어 강약을 가르오", "강약"),
    "ilji_state": ("일지는 그대 발밑 글자라, 가장 가까운 사람과의 일을 "
                   "보오", "십신"),
    "gwan_jae": ("그대를 누르는 관성(회사·규칙)과 그대가 쥐는 재성(돈·살림)을 "
                 "함께 보오", "십신"),
    "daeun_phase": ("대운은 십 년짜리라, 지금 그 십 년 가운데 어디쯤인지를 "
                    "보오", ""),
    "age_band": ("몇 번째 대운인지로 나이대를 나누오", ""),
    "zero_band": ("여덟 글자에 하나도 없는 기운을 보오. 없는 것이 곧 "
                  "모자란 것이오", "용신"),
    "sinsal_mark": ("옛사람이 글자 짝에 붙인 별명이오. 좋다 나쁘다를 정하는 게 "
                    "아니라 어디를 볼지 가리키는 이름이오", "신살"),
    "palace": ("년·월·일·시 네 기둥을 각각 윗대·부모·그대·자식으로 보오",
               "신살"),
    "next_daeun_tg": ("다음 대운에 무엇이 주인공인지를 보오", "십신"),
    "month_ji": ("월지는 태어난 계절이라, 여덟 글자 가운데 힘이 가장 "
                 "세오", "강약"),
    "year_ji": ("년지는 태어난 해의 아래 글자라, 자란 집안을 보오", ""),
    "top_ten_god": ("십신 열 가지 가운데 그대 사주에 가장 많은 것을 보오",
                    "십신"),
    "daeun_ten_god": ("지금 대운에 무엇이 주인공인지를 보오", "십신"),
    "strength": ("돕는 힘과 누르는 힘을 견줘 강약을 가르오", "강약"),
    "flow": ("그대 힘이 주로 어디로 가는지를 보오", "십신"),
    "season": ("태어난 계절이 타고난 바탕이오", "용신"),
    "weak_el": ("가장 모자란 기운을 보오", "용신"),
    "strong_el": ("가장 넘치는 기운을 보오", "용신"),
    "yongsin": ("그대에게 모자라서 채우면 좋은 기운을 용신이라 하오", "용신"),
    "day_gan": ("일간은 여덟 글자 가운데 그대 자신을 뜻하는 글자요", "십신"),
    "day_ji": ("일지는 그대 발밑 글자요", "십신"),
    "hour_known": ("태어난 시각을 모르면 시주를 세우지 않소. 없는 걸 지어내지 "
                   "않소", ""),
}


def axis_line(seen: str, axis: str) -> str:
    """관점 컷의 근거 — 관측에 그 **축의 뜻**을 붙인다."""
    got = AXIS.get(axis)
    if not got:
        return seen
    why, school = got
    out = "%s — %s" % (seen, why)
    src = SCHOOL.get(school, "")
    return out + (" 〔%s〕" % src if src else "")


# 갈래 — 지어낸 인용 대신 **어디서 온 규칙인지**만 댑니다.
SCHOOL = {
    "십신": "자평 명리 · 십신",
    "강약": "자평 명리 · 신강약",
    "용신": "자평 명리 · 용신",
    "절기": "만세력 · 절기",
    "신살": "신살 · 유파 확정표(docs/14)",
    "": "자평 명리",
}


def rule_of(name: str) -> str:
    """이 이름의 이치 한 줄. 모르면 빈 문자열."""
    return TEN_GOD.get(name) or GROUP.get(name) or RULE.get(name, "")


def strength_seen(f) -> str:
    """
    신강약을 **대 볼 수 있는 말**로. 점수는 안 냅니다.

    ★ 근거 줄 여섯 자리에서 `strength_score` 가 그대로 나가고 있었습니다
      (2026-09-25) — 「중화(3)」 「신강약 중화 · 점수 3」. 훅에서는 이미
      고쳐 두었는데(`bank.build_hook` 1단 머리말) 리포트·척추·고민축·
      혈액형·초상 다섯 자리가 남았습니다. 검사도 훅만 보고 있었습니다
      (`tests/test_source_hides_rules` 는 `build_hook` 만 훑습니다).

      그 수는 득령·득지에 점수를 매기고 비율을 백분율로 더한 **안에서
      쓰는 값**입니다. 3을 보면 손님은 몇부터 신강인지 묻게 되는데 그건
      우리 분기표요 (CLAUDE.md 「근거 문구에 연산자·문턱값 쓰기」).

      대신 **그 판정에 실제로 들어간 두 가지**를 냅니다. 둘 다 손님이
      만세력을 펴고 확인할 수 있습니다 — 태어난 달의 아래 글자가 내 편인가,
      내 발밑 글자가 내 편인가. 틀리면 손님이 그 자리에서 압니다.
    """
    #   ★ 「득령·득지」라 적지 않습니다 — 그 말은 풀이표에도 없는 술어라
    #     손님에게는 점수와 다를 바 없습니다 (docs/21).
    #
    #   ★ 「내 편」이라 쓰면 안 됩니다. 근거는 **손님에게 하는 말**인데 이
    #     줄은 캐릭터가 말하는 상자 안에 서니, 「내」가 일간(손님)이 아니라
    #     화자로 읽힙니다 — `tests/test_voice` 가 그 자리를 지킵니다.
    #     주인 없는 말로 적습니다.
    return "%s · 태어난 달 %s · 발밑 %s" % (
        f.strength,
        "받쳐 줌" if f.deuk_ryeong else "안 받쳐 줌",
        "받쳐 줌" if f.deuk_ji else "안 받쳐 줌")


def dedupe(sources: list) -> list:
    """
    한 장 안에서 같은 **이치**는 한 번만 **길게** 댑니다.

    ★ 손님이 짚었습니다 (2026-09-25) — 「왜 전체가 어렵고 와닿지 않지」.
      재 보니 무료 한 장의 근거 줄이 1,755자(본문의 12.2%)인데, 꼬리가
      아홉 가지뿐이고 그중 하나가 **한 장에 네 번** 깔려 있었습니다 —

          … — 힘이 가장 많이 가는 곳을 흐름이라 하오. 그 다음 차례
                글자가 여덟 글자에 있으면 … 〔자평 명리 · 십신〕

      앞쪽 관측은 컷마다 다른데 뒤 일흔 자는 글자 그대로 같습니다.
      근거는 **셈을 보이는 자리**라 관측이 본체이고, 이치는 그 셈을
      처음 보일 때 한 번 대면 됩니다. 두 번째부터는 근거가 아니라
      녹음입니다 — `report._LEDGER` 가 본문에서 잡는 것과 같은 자리요.

    ★ **떼지는 않습니다.** 처음에는 두 번째부터 이치를 통째로 뗐는데,
      `tests/test_evidence` 가 「이치 없는 근거 156회」로 잡았습니다. 그
      계약은 이렇게 적혀 있습니다 — 무엇을 보고 · 어떤 이치로 · 어디서 온
      규칙인지 **셋이 다 있어야 손님이 어디가 틀렸는지 짚을 수 있다.**
      떼면 그 줄은 다시 자료가 되고, 컷 하나만 보는 손님(공유 링크로
      들어온 사람)은 이치를 한 번도 못 봅니다.

      그래서 **줄입니다.** 두 번째부터는 이치의 첫 문장과 출처만 남깁니다 —
      「흐름」 이치가 70자에서 26자가 되고, 논증의 세 토막은 그대로 섭니다.

    ★ 차례에 안 기댑니다. 어느 줄이 긴 이치를 갖는지는 **줄의 글자**로
      정합니다(관측 부분을 열쇠로 골라) — 컷 차례는 캐릭터마다 갈리니
      차례로 정하면 스무 사람이 서로 다른 근거를 받습니다
      (`report` 의 「근거 줄의 seen 은 따로 셉니다」 머리말과 같은 까닭).
    """
    keep: dict = {}
    for i, s in enumerate(sources):
        if not s or " — " not in s:
            continue
        head, tail = s.split(" — ", 1)
        cur = keep.get(tail)
        if cur is None or head < cur[1]:
            keep[tail] = (i, head)
    winner = {i for i, _ in keep.values()}
    out = []
    for i, s in enumerate(sources):
        if not s or " — " not in s or i in winner:
            out.append(s)
            continue
        out.append(_short(s))
    return out


def _short(line: str) -> str:
    """이치를 첫 문장까지만. 출처 묶음표는 그대로 답니다."""
    head, tail = line.split(" — ", 1)
    src = ""
    if "〔" in tail:
        tail, src = tail.split("〔", 1)
        src = "〔" + src
    first = tail.split(". ", 1)[0].rstrip()
    if not first.endswith((".", "오", "소", "다", "네", "요")):
        first = first.rstrip(".") + "."
    return "%s — %s%s" % (head, first, (" " + src) if src else "")


def line(seen: str, rule_key: str = "", school: str = "") -> str:
    """
    근거 한 줄을 **관측 · 이치 · 출처** 로 짓는다.

        line("庚일간 · 나무가 거의 없음 · 식신", "식신", "십신")
        → "庚일간 · 나무가 거의 없음 · 식신
           — 식신은 그대가 내놓는 자리라, 만들고 먹는 복을 보오
           〔자평 명리 · 십신〕"

    ★ 이치를 모르면 **붙이지 않습니다.** 아무 규칙이나 갖다 붙이면
      그건 근거가 아니라 장식입니다.
    """
    why = rule_of(rule_key) if rule_key else ""
    out = seen
    if why:
        out += " — " + why
    src = SCHOOL.get(school or ("십신" if rule_key in TEN_GOD or
                                rule_key in GROUP else ""), "")
    if why and src:
        out += " 〔%s〕" % src
    return out
