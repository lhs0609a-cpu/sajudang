# 2026-09-09 취약점 수정 출시

## 포함 범위

1차 수정(`b5d542f`, 문서 25)의 계산 정책 격리, 구매 복원·환불·무료 재발행, 날짜별 명식 재계산, 조회 오류·재시도를 서버와 웹에 함께 반영한다.

추가 변경:
- 결과 표지와 본문에 확인 질문·계산 근거·오늘 할 행동·해석의 한계를 노출한다. 고양이는 읽는 속도를 강요하지 않는 안내로 배치한다.
- 긴 본문은 제목별로 펼쳐 읽는다. 첫 항목은 열어 두고, 인쇄 때 전체 내용을 연 뒤 화면 상태를 복원한다. 본문 16px, 근거 14px, 항목 터치 높이 56px로 정리했다.
- 구매 전 현재 상품의 추가 입력 목록을 실제 생성 결과에서 합산해 안내한다. 미입력 시 제공되지 않는 추가 해석과 제공되는 명식 본문을 구분한다.
- 관리자 점수의 명칭·등급을 실제 자동 검사 범위로 바꾸고 표본을 공개한다. API의 사용자 만족도·전환율은 미측정(null)이다. 글의 분량이나 단어 차이를 정확도나 만족도로 표현하지 않는다.
- 시각 미상에서도 보이는 결과 화면의 고정 ‘여덟 글자’ 표현을 ‘명식’으로 정리했다.
- 일반 서버 오류는 카드가 청구되지 않았다고 단정하지 않는다. 결제 도중 오류가 나면 구매 내역과 카드 승인 내역을 확인하도록 안내한다.

## 검증과 한계

- 전체 Python 검사: 1,268 통과, 2 건너뜀, 기존 초상화 정렬 3 실패. 이후 추가한 상품 입력 회귀 검사와 관리자 점수 묶음 21개 통과.
- Next 프로덕션 빌드 및 타입 검사 통과.
- 이전 1만 명 합성 명식 시뮬레이션은 문서 24의 기준 코드에서 실행한 결과이며, 이번 코드로 재실행했다고 주장하지 않는다. 실제 1만 명의 동시 접속 성능·이해도·결제율을 측정한 것이 아니다.
- 초상화는 생성본에 투명 배경 대신 체크무늬가 포함되어 교체하지 않았다. 생성 도구 외 배경 제거·규격 정렬 도구 사용에 대한 응답을 기다리고 있다. 정렬 실패 테스트를 삭제하거나 통과로 바꾸지 않았다.
- 조건별 본문 전체의 의미 검토, 실제 사용자 이해도·결제 만족도 검증은 완료되지 않았다. 자동 검사 통과와 구분한다.
- 구독 자동결제는 기존 승인 게이트를 유지한다. 실제 PG 계약 확인 및 실결제 검증을 수행하지 않았다.

따라서 이 출시는 기능 취약점 수정본이며, 전체 리디자인·초상화 교체·실사용자 검증의 100% 완료 선언이 아니다.
