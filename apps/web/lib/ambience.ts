"use client";
import type {RefObject} from 'react';
/** Root BackgroundMusic owns the shared track; scene changes never restart it. */
export function useAmbience(_bed:string|undefined,_ref:RefObject<Element|null>) {}